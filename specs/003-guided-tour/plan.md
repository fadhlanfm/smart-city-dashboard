# Implementation Plan: Guided Tour

See the implementation_plan.md artifact for the full plan.

## Steps:
1. Install `react-joyride`.
2. Create `<GuidedTour />` component with joyride state.
3. Update `<Header />` to add the Tutorial button.
4. Add IDs/classes to target elements (Sidebar, Search, Table, Add Asset).
5. Render `<GuidedTour />` in `app/(dashboard)/layout.tsx`.
